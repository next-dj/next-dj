"""Change view — delegates to the `admin_form` composite component.

URL kwargs `app_label` / `model_name` / `pk` flow into the component's
context function and action handlers via dependency injection.
"""
