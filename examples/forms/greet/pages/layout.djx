<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Greet</title>
    <style>
        body { font-family: system-ui, sans-serif; max-width: 42rem; margin: 0 auto; padding: 1rem; }
        nav { display: flex; align-items: center; margin-bottom: 1.5rem; padding-bottom: 0.5rem; border-bottom: 1px solid #ccc; }
        nav a { margin-right: 1rem; }
        nav a.active { font-weight: bold; }
        nav .spacer { flex: 1; }
        nav form { display: inline; margin: 0; }
        nav button { cursor: pointer; }
    </style>
</head>
<body>
    <nav>
        <a href="/home/" {% if request.path == '/home/' %}class="active"{% endif %}>Home</a>
        <a href="/login/" {% if request.path == '/login/' %}class="active"{% endif %}>Login</a>
        <a href="/settings/" {% if request.path == '/settings/' %}class="active"{% endif %}>Settings</a>
        <span class="spacer"></span>
        {% if request.session.username %}
            {% form @action="logout" %}
                <button type="submit">Logout</button>
            {% endform %}
        {% endif %}
    </nav>

    {% block template %}{% endblock template %}
</body>
</html>
